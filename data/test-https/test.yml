version: '3'

services:

  nginx:
    image: nginx:latest
    command: "${NDL_REPO_ROOT}/data/test-https/init-nginx-text.sh"
    ports:
      - "80:80"
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - ./www:/www
      - ../certbot/conf:/etc/letsencrypt
      - ../certbot/www:/var/www/certbot
